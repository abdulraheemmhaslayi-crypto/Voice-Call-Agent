# TODO

- [ ] Add timeout + retry UI for `/workflow/[workflowId]/run/[runId]` run-details fetch (fix long “loading”)
- [ ] (Optional follow-up) Add polling after timeout so run details eventually appear without refresh
- [ ] Test by triggering a run with missing microphone and verifying the run-details page no longer stays stuck

